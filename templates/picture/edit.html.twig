{% extends 'base.html.twig' %}

{% block title %}Editer l'image{% endblock %}

{% block body %}
    <h1>Editer l'image</h1>

    {{ include('picture/_form.html.twig', {'button_label': 'Update'}) }}

    {% if is_granted("ROLE_ADMIN") %}
        {{ include('picture/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
